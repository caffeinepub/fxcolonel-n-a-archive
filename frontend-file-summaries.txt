{
  "frontend/src/index.css": "Global OKLCH dark navy/gold theme with Roboto as the sole font family throughout (replacing DM Sans and DM Serif Display), applied to body, headings, inputs, Quill editor, and article prose styles.",
  "frontend/tailwind.config.js": "Extended Tailwind config with Roboto as both the default sans and serif font (replacing DM Sans and DM Serif Display), plus all existing color tokens and shadow utilities.",
  "frontend/src/App.tsx": "Root app component using TanStack Router with createRootRoute/createRouter, Layout shell with Header/Footer/Outlet, and routes for LandingPage, AdminDashboard, and ApiReferencePage.",
  "frontend/src/main.tsx": "Application entry point mounting App with QueryClientProvider and InternetIdentityProvider wrappers.",
  "frontend/src/utils/formatters.ts": "Utility functions for stripping HTML tags, formatting bigint nanosecond timestamps to readable dates, truncating text, and relative date formatting.",
  "frontend/src/utils/slugify.ts": "Utility function that converts a title string to a URL-safe lowercase hyphenated slug.",
  "frontend/src/hooks/useQueries.ts": "All React Query hooks for backend interactions — fixed `useTogglePublishArticle` to properly `await` the backend call, added `latestArticles` and `searchArticles` invalidation to all publish/unpublish mutations, and ensured legacy `usePublishArticle`/`useUnpublishArticle` hooks also invalidate all relevant query keys.",
  "frontend/src/components/Header.tsx": "Sticky header with the FX Colonel Pips Network logo image (from external URL) replacing the previous text-only brand, plus desktop nav, mobile hamburger menu, and Internet Identity login/logout button.",
  "frontend/src/components/Footer.tsx": "Dark footer with the FX Colonel Pips Network logo image, navigation links, trading disclaimer, and copyright notice — the caffeine.ai attribution has been removed.",
  "frontend/src/components/ArticleCard.tsx": "Article card component — fixed `coverImageUrl` usage by calling `.getDirectURL()` on the `ExternalBlob` instance before passing to `<img src>`, since the field is now `ExternalBlob | undefined` not a string.",
  "frontend/src/components/ArticleDetailModal.tsx": "Responsive article detail modal (bottom-sheet on mobile, centered popup on desktop) — fixed swipe-to-close so drag handlers are only attached to the drag handle and close-button header row, not the scrollable content area; the content area uses `overscroll-contain` and `touch-none` is applied only to the draggable zones, preventing scroll events from triggering the dismiss gesture.",
  "frontend/src/components/ConfirmationModal.tsx": "Reusable confirmation dialog using CSS opacity/scale transitions via React state, with destructive action and cancel buttons.",
  "frontend/src/components/ProfileSetupModal.tsx": "First-login profile setup modal using CSS transitions for fade/slide/scale animations, prompting authenticated users for their display name.",
  "frontend/src/components/ArticleEditor.tsx": "Full article editor — fixed the critical bug where clicking \"Publish\" only saved content but never called `togglePublishArticle`; now after create/update it calls `togglePublishArticle(id, ArticleStatus.published)` when publishing, shows inline success/error feedback banners, and delays `onSaved()` briefly to display the confirmation message.",
  "frontend/src/components/ArticleListView.tsx": "Admin article list — refactored to use `useTogglePublishArticle` directly (instead of separate publish/unpublish hooks), added inline success/error feedback banners after publish toggle and delete actions, and improved error handling with user-visible messages.",
  "frontend/src/pages/LandingPage.tsx": "Public landing page with hero section that now features a layered blur effect — the background image sits behind a `backdrop-blur-sm` layer and a dark overlay, with decorative gold glow orbs for depth when no image is set; hero text and search bar remain fully legible via `z-10`.",
  "frontend/src/pages/AdminDashboard.tsx": "Admin dashboard page — updated to match the new `ArticleListView` props interface (removed `onDelete`, `onCreate`, `publishingId`, `deletingId`; moved \"New Article\" button into the dashboard header area), changed `editingArticle` state type from `Article | null` to `Article | undefined` to match `ArticleEditor`'s optional `article` prop, and updated `ArticleEditor` to use the new `onSaved` callback.",
  "frontend/src/pages/ApiReferencePage.tsx": "Developer API reference page fully updated to match the current backend interface — replaced stale `publishArticle`/`unpublishArticle` with `togglePublishArticle`, added `getHeroImage`/`updateHeroImage`, `registerFirstAdmin`, `isCallerAdmin`, `getCallerUserRole`, `assignCallerUserRole`, and all user profile endpoints; corrected `Article` type to use `ExternalBlob` for `coverImageUrl`/`inlineImages`, added a Notes & Gotchas section, and updated all examples and descriptions.",
  "frontend/src/components/HeroImageSettings.tsx": "Admin panel component for managing the hero banner image, supporting URL input and file upload (via native FileReader API as data URL), with live preview, loading states, and success/error feedback. Removed the broken ExternalBlob import and replaced with browser-native file reading.",
  "frontend/index.html": "HTML entry point loading Roboto from Google Fonts (replacing DM Sans/DM Serif Display) and using the FX Colonel Pips Network logo URL as the favicon."
}